<!DOCTYPE html>
<html>
  <% include _includes/head.ejs %>
  <body>
    <h1><%= title %></h1>
    <ul>
    <% list.forEach(function (item) {%>
      <li> <a href="/article/<%= item.id %>"><%= item.title %></a></li>
    <% }) %>
    </ul>
  </body>
</html>
